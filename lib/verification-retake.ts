/**
 * Copy and UI timing for verification retake cooldown after a first-attempt failure.
 * Must match `RETAKE_COOLDOWN_MS` in `convex/vetting/scoring.ts` (same minute length).
 */
export const VERIFICATION_RETAKE_COOLDOWN_MINUTES = 10;
